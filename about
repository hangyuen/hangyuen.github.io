<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>About — Hang Yuan</title>
<link rel="icon" type="image/png" href="hy.png">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@500&display=swap" rel="stylesheet">
<style>
  :root{
    --bg: #000000;
    --white: #ffffff;
    --dim: #b7b7b7;
    --card-bg: #ffffff;
    --card-text: #111111;
    --card-dim: #9a9a9a;
    --card-panel: #f2f2f2;
    --link: #2f6fed;
  }

  *{ margin:0; padding:0; box-sizing:border-box; }
  html, body{ height:100%; }

  body{
    background:var(--bg);
    color:var(--white);
    font-family:'Inter', sans-serif;
    min-height:100vh;
    display:flex;
    align-items:center;
    justify-content:center;
    padding:48px 20px 140px;
  }

  a{ color:inherit; text-decoration:none; }

  .window{
    width:100%;
    max-width:600px;
    background:var(--card-bg);
    color:var(--card-text);
    border-radius:18px;
    overflow:hidden;
    box-shadow:0 40px 90px rgba(0,0,0,0.55);
  }

  .titlebar{
    display:flex;
    align-items:center;
    gap:8px;
    padding:16px 20px;
    border-bottom:1px solid #ececec;
  }
  .dot{ width:12px; height:12px; border-radius:50%; }
  .dot.red{
    background:#ff5f57;
    position:relative;
    transition:filter 0.15s ease;
  }
  .dot.red::after{
    content:'×';
    position:absolute;
    inset:0;
    display:flex;
    align-items:center;
    justify-content:center;
    font-size:11px;
    line-height:1;
    color:rgba(75,0,0,0.65);
    opacity:0;
    transition:opacity 0.15s ease;
  }
  .dot.red:hover{ filter:brightness(0.95); }
  .dot.red:hover::after{ opacity:1; }

  .content{
    padding:32px 32px 40px;
  }

  h1{
    font-family:-apple-system, BlinkMacSystemFont, "SF Pro Display", "Helvetica Neue", Arial, sans-serif;
    font-size:34px;
    font-weight:700;
    letter-spacing:-0.02em;
    margin-bottom:24px;
  }

  .profile{
    display:flex;
    gap:24px;
    align-items:flex-start;
    margin-bottom:20px;
  }

  .photo{
    width:130px;
    height:130px;
    border-radius:14px;
    object-fit:cover;
    background:#e4e4e4;
    flex-shrink:0;
  }

  .fields{
    flex:1;
    min-width:0;
  }

  .field{
    display:flex;
    align-items:baseline;
    gap:12px;
    padding:12px 0;
    border-bottom:1px solid #ececec;
  }
  .field:first-child{ padding-top:2px; }

  .field-label{
    font-family:'JetBrains Mono', monospace;
    font-size:13px;
    letter-spacing:0.02em;
    color:var(--card-text);
    width:72px;
    flex-shrink:0;
  }

  .field-value{
    font-size:16px;
    color:var(--card-dim);
  }

  .field-value.link{
    color:var(--link);
  }

  .bio{
    background:var(--card-panel);
    border-radius:14px;
    padding:22px 24px;
    margin-top:24px;
  }

  .bio p{
    font-size:16px;
    line-height:1.55;
    color:#222;
  }
  .bio p + p{ margin-top:16px; }

  .map-wrap{
    margin-top:16px;
    border-radius:12px;
    overflow:hidden;
    border:1px solid #e2e2e2;
  }
  .map-wrap iframe{
    display:block;
    width:100%;
    height:180px;
    border:0;
    filter:grayscale(0.15);
  }
  .map-caption{
    font-size:13px;
    color:#8a8a8a;
    margin-top:8px;
  }

  /* DOCK — same as index.html */
  .dock{
    position:fixed;
    left:50%; bottom:34px;
    transform:translateX(-50%);
    z-index:10;
    display:flex;
    align-items:center;
    gap:14px;
    padding:12px 16px;
    background:rgba(255,255,255,0.08);
    border:1px solid rgba(255,255,255,0.14);
    border-radius:24px;
    backdrop-filter:blur(16px);
  }
  .dock-item{
    width:64px;
    height:64px;
    border-radius:16px;
    display:flex;
    align-items:center;
    justify-content:center;
    background:#151515;
    border:1px solid #2a2a2a;
    transition:transform 0.15s ease;
    position:relative;
  }
  .dock-item:hover{ transform:translateY(-6px); }
  .dock-item.avatar{
    background:linear-gradient(135deg, #444, #111);
    padding:0;
  }
  .dock-item.avatar img{
    width:100%; height:100%; object-fit:cover; border-radius:16px;
  }
  .dock-item.brand{ background:transparent; border:none; padding:0; }
  .dock-item.brand img{
    width:100%; height:100%; object-fit:cover; border-radius:16px;
  }
  .dock-divider{
    width:1px; height:36px;
    background:rgba(255,255,255,0.16);
    margin:0 2px;
  }

  @media (max-width:640px){
    body{ padding:32px 16px 130px; }
    .content{ padding:24px 20px 32px; }
    h1{ font-size:28px; margin-bottom:20px; }
    .profile{ gap:16px; }
    .photo{ width:100px; height:100px; }
    .field-value{ font-size:14px; }
    .bio{ padding:18px; }
    .bio p{ font-size:14px; }
    .dock{ gap:10px; padding:10px 12px; bottom:20px; }
    .dock-item{ width:46px; height:46px; }
  }
</style>
</head>
<body>

<div class="window">
  <div class="titlebar">
    <span class="dot red"></span>

  </div>

  <div class="content">
    <h1>About Me</h1>

    <div class="profile">
      <img class="photo" src="hangyuan.jpg" alt="Hang Yuan">
      <div class="fields">
        <div class="field">
          <span class="field-label">NAME</span>
          <span class="field-value">Hang Yuan</span>
        </div>
        <div class="field">
          <span class="field-label">POSITION</span>
          <span class="field-value">Assistant Professor at USI</span>
        </div>
        <div class="field">
          <span class="field-label">MAIL</span>
          <a class="field-value link" href="mailto:hyuan@usi.edu">hyuan@usi.edu</a>
        </div>
      </div>
    </div>

    <div class="bio">
      <p>I'm Hang /hʌŋ/, an interdisciplinary designer and creative technologist based in Evansville, IN — or anywhere.</p>
      <p>I mainly work with digital mediums to create interactive experiences and visuals that resonate with people.</p>
      <p>Currently, I teach digital media and game design courses as an Assistant Professor at the University of Southern Indiana.</p>

      <div class="map-wrap">
        <iframe
          src="https://www.openstreetmap.org/export/embed.html?bbox=-87.6844%2C37.9394%2C-87.4344%2C38.0794&layer=mapnik&marker=38.0294%2C-87.5344"
          loading="lazy"
          referrerpolicy="no-referrer-when-downgrade"
          title="Map of Evansville, IN">
        </iframe>
      </div>
      <div class="map-caption">Evansville, IN</div>
    </div>
  </div>
</div>

<div class="dock">
  <a class="dock-item avatar" href="index.html" data-tip="Home"><img src="hangyuan.jpg" alt="Hang Yuan"></a>
  <a class="dock-item brand" href="mailto:hyuan@usi.edu" data-tip="Contact Me">
    <img src="Mail.png" alt="Mail">
  </a>
  <div class="dock-divider"></div>
  <a class="dock-item brand" href="https://www.instagram.com/hang_yuan_/" target="_blank" rel="noopener" data-tip="Instagram">
    <img src="Instagram.svg" alt="Instagram">
  </a>
  <a class="dock-item brand" href="https://www.linkedin.com/in/-hangyuan-" target="_blank" rel="noopener" data-tip="LinkedIn">
    <img src="LinkedIn.png" alt="LinkedIn">
  </a>
</div>

</body>
</html>
